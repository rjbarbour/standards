# Repository Instructions

- Use ISO 8601 calendar dates (`YYYY-MM-DD`) in standards metadata blocks.
- Render the human author name as `R. J. Babour` in standards metadata unless the user explicitly asks for a different form.
- Treat the top-level standards files in `OSSS/`, `README.llms-vNext/`, `AXON/`, and `cloakd/` as the canonical sources. Files generated into `ai-open-standards/src/pages/` are build artifacts.
